interface:
  display_name: "MultiAgentor CLI"
  short_description: "Guide setup, browser identity choices, and scenario runs"
  default_prompt: "Use $multiagentor to check my setup, guide me through selecting or creating a browser identity, and run an official scenario."

policy:
  allow_implicit_invocation: true
